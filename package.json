
{
  "name": "event-registration",
  "version": "1.0.0",
  "description": "Simple event registration page + server that appends registrations to a text file",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}